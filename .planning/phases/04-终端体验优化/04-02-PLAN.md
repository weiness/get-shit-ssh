---
phase: 04-终端体验优化
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - frontend/src/components/Terminal/TerminalPane.tsx
  - frontend/src/components/Terminal/TerminalTabBar.tsx
  - frontend/src/components/Host/HostList.tsx
autonomous: true
requirements: [TERM-01, TERM-02, UX-03]

must_haves:
  truths:
    - "终端区域右键弹出自定义菜单，包含「复制」「粘贴」「清空」三项"
    - "无选中文本时「复制」项视觉置灰且不可点击"
    - "有选中文本时「复制」项正常可用，点击后文本写入剪贴板"
    - "「粘贴」点击后剪贴板内容输入到终端"
    - "「清空」点击后终端屏幕清空"
    - "Tab 鼠标悬停显示 tooltip，包含主机名和 user@host:port"
  artifacts:
    - path: "frontend/src/components/Terminal/TerminalPane.tsx"
      provides: "右键菜单 UI + 复制/粘贴/清空逻辑"
      contains: "onContextMenu"
    - path: "frontend/src/components/Terminal/TerminalTabBar.tsx"
      provides: "Tab tooltip 显示完整主机信息"
      contains: "title="
    - path: "frontend/src/components/Host/HostList.tsx"
      provides: "创建 TermTab 时填充 hostUser/hostAddr 字段"
      contains: "hostUser"
  key_links:
    - from: "frontend/src/components/Terminal/TerminalPane.tsx"
      to: "wailsjs/runtime/runtime"
      via: "ClipboardGetText/ClipboardSetText import"
      pattern: "ClipboardGetText|ClipboardSetText"
    - from: "frontend/src/components/Host/HostList.tsx"
      to: "frontend/src/components/Terminal/TerminalTabBar.tsx"
      via: "TermTab 接口的 hostUser/hostAddr 字段"
      pattern: "hostUser.*hostAddr"
---

<objective>
实现终端右键上下文菜单（复制/粘贴/清空）和 Tab tooltip 完整主机信息显示。

Purpose: 提供终端基本操作的便捷入口，补全 Tab 信息展示。
Output: TerminalPane 右键菜单功能完整，Tab hover 显示 user@host:port。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-终端体验优化/04-RESEARCH.md
@.planning/phases/04-终端体验优化/04-01-SUMMARY.md

@frontend/src/components/Terminal/TerminalPane.tsx
@frontend/src/components/Terminal/TerminalTabBar.tsx
@frontend/src/components/Host/HostList.tsx
@frontend/src/components/FileManager/SFTPBrowser.tsx
@frontend/wailsjs/runtime/runtime.d.ts

<interfaces>
<!-- Wails Clipboard API — 从 runtime.d.ts 确认 -->
From frontend/wailsjs/runtime/runtime.d.ts:
```typescript
export function ClipboardGetText(): Promise<string>;
export function ClipboardSetText(text: string): Promise<boolean>;
```

<!-- TermTab 接口 — Plan 01 不修改此接口，Plan 02 需要扩展 -->
From frontend/src/components/Terminal/TerminalTabBar.tsx:
```typescript
export interface TermTab {
  termID: string
  sessionID: string
  hostID: string
  hostName: string
  isHome?: boolean
  status?: 'connected' | 'disconnected'
}
```

<!-- Host 类型 — 用于从 hosts 数组提取 username/host/port -->
From frontend/src/types/host.ts:
```typescript
export interface Host {
  id: string
  name: string
  groupName: string
  host: string
  port: number
  username: string
  authType: 'password' | 'key' | ''
  keyId: string
  createdAt: number
  updatedAt: number
}
```

<!-- SFTPBrowser contextMenu 模式 — 参考实现 -->
From frontend/src/components/FileManager/SFTPBrowser.tsx:
```typescript
interface ContextMenu {
  x: number
  y: number
  entry: FileEntry | null
}
// state: const [contextMenu, setContextMenu] = useState<ContextMenu | null>(null)
// 容器 div: onContextMenu={(e) => { e.preventDefault(); setContextMenu({...}) }}
// 关闭: useEffect 监听 document click → setContextMenu(null)
// 渲染: fixed 定位, z-index 9999, bg-white dark:bg-gray-800 rounded-lg shadow-xl
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: TermTab 接口扩展 + HostList 填充 + Tab tooltip</name>
  <files>frontend/src/components/Terminal/TerminalTabBar.tsx, frontend/src/components/Host/HostList.tsx</files>
  <read_first>
    - frontend/src/components/Terminal/TerminalTabBar.tsx（TermTab 接口 line 3-10，button 渲染 line 27-57）
    - frontend/src/components/Host/HostList.tsx（handleConnect line 73-91 创建 newTab，handleReconnect line 93-109 创建 newTab，handleAddHomeTab line 141-146 创建 home tab）
    - frontend/src/types/host.ts（Host 接口，含 username/host/port 字段）
  </read_first>
  <action>
1. 修改 TerminalTabBar.tsx 中的 TermTab 接口，添加两个可选字段：
   ```typescript
   export interface TermTab {
     termID: string
     sessionID: string
     hostID: string
     hostName: string
     hostUser?: string    // SSH 用户名，如 "root"
     hostAddr?: string    // host:port，如 "192.168.1.1:22"
     isHome?: boolean
     status?: 'connected' | 'disconnected'
   }
   ```

2. 在 TerminalTabBar.tsx 的 tab button 元素上添加 title 属性（在 `onClick` 之前）：
   ```typescript
   <button
     key={tab.termID}
     title={tab.isHome ? '新连接' : tab.hostUser && tab.hostAddr ? `${tab.hostName}\n${tab.hostUser}@${tab.hostAddr}` : tab.hostName}
     onClick={() => onSelect(tab.termID)}
     ...
   ```

3. 修改 HostList.tsx 的 handleConnect 函数（约 line 79），在创建 newTab 时填充 hostUser 和 hostAddr。需要从 hosts 数组中查找对应 host 对象：
   在 `const termID = await openTerminal(...)` 之后、`const newTab: TermTab = ...` 之前，添加：
   ```typescript
   const hostObj = hosts.find(h => h.id === hostID)
   ```
   然后修改 newTab 创建：
   ```typescript
   const newTab: TermTab = {
     termID, sessionID, hostID, hostName, status: 'connected',
     hostUser: hostObj?.username,
     hostAddr: hostObj ? `${hostObj.host}:${hostObj.port}` : undefined,
   }
   ```

4. 同样修改 handleReconnect 函数（约 line 100），在创建 newTab 时填充：
   在 `const termID = await openTerminal(...)` 之后添加：
   ```typescript
   const hostObj = hosts.find(h => h.id === tab.hostID)
   ```
   修改 newTab：
   ```typescript
   const newTab: TermTab = {
     termID, sessionID, hostID: tab.hostID, hostName: tab.hostName, status: 'connected',
     hostUser: hostObj?.username,
     hostAddr: hostObj ? `${hostObj.host}:${hostObj.port}` : undefined,
   }
   ```
  </action>
  <verify>
    <automated>cd D:/code/github/get-shit-ssh/frontend && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - TerminalTabBar.tsx 的 TermTab 接口包含 `hostUser?: string` 字段
    - TerminalTabBar.tsx 的 TermTab 接口包含 `hostAddr?: string` 字段
    - TerminalTabBar.tsx 的 tab button 包含 `title={tab.isHome ? '新连接'` 开头的 title 属性
    - HostList.tsx handleConnect 中包含 `const hostObj = hosts.find(h => h.id === hostID)`
    - HostList.tsx handleConnect 的 newTab 包含 `hostUser: hostObj?.username`
    - HostList.tsx handleConnect 的 newTab 包含 `hostAddr: hostObj ?`
    - HostList.tsx handleReconnect 的 newTab 同样包含 hostUser 和 hostAddr
    - TypeScript 编译无错误
  </acceptance_criteria>
  <done>Tab hover 显示 tooltip 包含主机名和 user@host:port 信息，home tab 显示"新连接"</done>
</task>

<task type="auto">
  <name>Task 2: 终端右键上下文菜单（复制/粘贴/清空）</name>
  <files>frontend/src/components/Terminal/TerminalPane.tsx</files>
  <read_first>
    - frontend/src/components/Terminal/TerminalPane.tsx（Plan 01 修改后的版本，包含 themeStore 订阅）
    - frontend/src/components/FileManager/SFTPBrowser.tsx（contextMenu 模式参考：state line 29, 事件 line 174-178, 关闭 line 74-79, 渲染 line 296-378）
    - frontend/wailsjs/runtime/runtime.d.ts（ClipboardGetText/ClipboardSetText 签名）
  </read_first>
  <action>
1. 在 TerminalPane.tsx 顶部添加 import：
   ```typescript
   import { ClipboardGetText, ClipboardSetText } from '../../../wailsjs/runtime/runtime'
   import { Copy, ClipboardPaste, Eraser } from 'lucide-react'
   ```
   注意：lucide-react 已在项目中使用（TerminalTabBar 等文件已 import），无需安装。

2. 在组件函数体内（现有 state 声明之后）添加右键菜单 state：
   ```typescript
   const [contextMenu, setContextMenu] = useState<{ x: number; y: number; hasSelection: boolean } | null>(null)
   ```

3. 添加关闭右键菜单的 useEffect（在现有 useEffect 块之后）：
   ```typescript
   // Close context menu on outside click
   useEffect(() => {
     if (!contextMenu) return
     const handler = () => setContextMenu(null)
     document.addEventListener('click', handler)
     return () => document.removeEventListener('click', handler)
   }, [contextMenu])
   ```

4. 添加三个操作 handler 函数（在 return 之前）：
   ```typescript
   const handleCopy = async () => {
     const text = termRef.current?.getSelection()
     if (text) await ClipboardSetText(text)
     setContextMenu(null)
   }

   const handlePaste = async () => {
     const text = await ClipboardGetText()
     if (text && termRef.current) termRef.current.paste(text)
     setContextMenu(null)
   }

   const handleClear = () => {
     termRef.current?.clear()
     setContextMenu(null)
   }
   ```

5. 在容器 div（最外层 `<div className={styles.container}` 元素）上添加 onContextMenu 事件：
   ```typescript
   <div
     className={styles.container}
     style={{ display: visible ? 'flex' : 'none', position: 'relative', backgroundColor: theme === 'dark' ? '#1e1e2e' : '#fdf6e3' }}
     onClick={() => { termRef.current?.focus(); setContextMenu(null) }}
     onContextMenu={(e) => {
       e.preventDefault()
       const hasSelection = (termRef.current?.getSelection()?.length ?? 0) > 0
       setContextMenu({ x: e.clientX, y: e.clientY, hasSelection })
     }}
   >
   ```
   注意：原有的 `onClick={() => termRef.current?.focus()}` 需要同时关闭菜单。

6. 在容器 div 内部、`{disconnected && onReconnect && (` 之前，添加右键菜单渲染：
   ```tsx
   {/* Context menu */}
   {contextMenu && (
     <div
       style={{ position: 'fixed', left: contextMenu.x, top: contextMenu.y, zIndex: 9999 }}
       className="bg-white dark:bg-gray-800 border border-gray-200 dark:border-gray-700 rounded-lg shadow-xl py-1 min-w-36"
       onClick={(e) => e.stopPropagation()}
     >
       <button
         disabled={!contextMenu.hasSelection}
         onClick={handleCopy}
         className="w-full flex items-center gap-2 px-3 py-1.5 text-sm hover:bg-gray-100 dark:hover:bg-gray-700 text-left disabled:opacity-40 disabled:cursor-not-allowed disabled:hover:bg-transparent dark:disabled:hover:bg-transparent"
       >
         <Copy size={13} /> 复制
       </button>
       <button
         onClick={handlePaste}
         className="w-full flex items-center gap-2 px-3 py-1.5 text-sm hover:bg-gray-100 dark:hover:bg-gray-700 text-left"
       >
         <ClipboardPaste size={13} /> 粘贴
       </button>
       <div className="my-1 border-t border-gray-200 dark:border-gray-700" />
       <button
         onClick={handleClear}
         className="w-full flex items-center gap-2 px-3 py-1.5 text-sm hover:bg-gray-100 dark:hover:bg-gray-700 text-left"
       >
         <Eraser size={13} /> 清空
       </button>
     </div>
   )}
   ```
  </action>
  <verify>
    <automated>cd D:/code/github/get-shit-ssh/frontend && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - TerminalPane.tsx 包含 `import { ClipboardGetText, ClipboardSetText } from '../../../wailsjs/runtime/runtime'`
    - TerminalPane.tsx 包含 `import { Copy, ClipboardPaste, Eraser } from 'lucide-react'`
    - TerminalPane.tsx 包含 `useState<{ x: number; y: number; hasSelection: boolean } | null>(null)`
    - TerminalPane.tsx 包含 `onContextMenu` 事件处理器，内含 `e.preventDefault()`
    - TerminalPane.tsx 包含 `termRef.current?.getSelection()` 用于检测选中状态
    - TerminalPane.tsx 包含 `handleCopy` 函数，调用 `ClipboardSetText`
    - TerminalPane.tsx 包含 `handlePaste` 函数，调用 `ClipboardGetText` 和 `termRef.current.paste(text)`
    - TerminalPane.tsx 包含 `handleClear` 函数，调用 `termRef.current?.clear()`
    - 复制按钮包含 `disabled={!contextMenu.hasSelection}` 和 `disabled:opacity-40`
    - 菜单容器包含 `position: 'fixed'` 和 `zIndex: 9999`
    - 包含 document click 关闭菜单的 useEffect
    - TypeScript 编译无错误
  </acceptance_criteria>
  <done>终端右键弹出菜单含复制/粘贴/清空三项，无选中文本时复制置灰，复制/粘贴使用 Wails clipboard API，清空调用 terminal.clear()</done>
</task>

</tasks>

<verification>
1. `cd frontend && npx tsc --noEmit` — TypeScript 编译无错误
2. 手动验证：终端区域右键弹出自定义菜单，不出现浏览器默认菜单
3. 手动验证：无选中文本时「复制」按钮置灰（opacity-40），不可点击
4. 手动验证：选中文本后右键，「复制」可用，点击后文本进入剪贴板
5. 手动验证：「粘贴」点击后剪贴板内容输入终端
6. 手动验证：「清空」点击后终端屏幕清空
7. 手动验证：Tab hover 显示 tooltip，包含主机名和 user@host:port
</verification>

<success_criteria>
- 右键菜单包含复制、粘贴、清空三项操作
- 无选中文本时复制按钮 disabled + opacity-40 视觉置灰
- 复制使用 Wails ClipboardSetText，粘贴使用 ClipboardGetText + terminal.paste()
- 清空调用 terminal.clear()
- 点击菜单外部或执行操作后菜单关闭
- Tab tooltip 显示 "主机名\nuser@host:port" 格式
- TypeScript 编译通过
</success_criteria>

<output>
After completion, create `.planning/phases/04-终端体验优化/04-02-SUMMARY.md`
</output>
