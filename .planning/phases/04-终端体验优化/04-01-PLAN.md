---
phase: 04-终端体验优化
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/Terminal/TerminalPane.tsx
  - frontend/src/components/Terminal/TerminalPane.module.css
  - frontend/src/components/Terminal/TerminalTabBar.tsx
  - frontend/src/components/Terminal/QuickConnectPane.tsx
autonomous: true
requirements: [TERM-03, TERM-04]

must_haves:
  truths:
    - "亮色主题下终端背景为 Solarized Light 护眼色 #fdf6e3，文字为 #657b83"
    - "暗色主题下终端保持 Catppuccin Mocha 配色 #1e1e2e/#cdd6f4"
    - "切换主题后终端实时更新颜色，无需重新连接"
    - "激活 tab 背景色与终端背景色一致（亮色 #fdf6e3，暗色 #1e1e2e）"
    - "QuickConnectPane 背景色与终端主题同步"
  artifacts:
    - path: "frontend/src/components/Terminal/TerminalPane.tsx"
      provides: "动态 xterm 主题 + themeStore 订阅"
      contains: "useThemeStore"
    - path: "frontend/src/components/Terminal/TerminalPane.module.css"
      provides: "动态背景色（不再硬编码 #1e1e2e）"
    - path: "frontend/src/components/Terminal/TerminalTabBar.tsx"
      provides: "激活 tab 动态背景色"
      contains: "useThemeStore"
    - path: "frontend/src/components/Terminal/QuickConnectPane.tsx"
      provides: "背景色与终端主题同步"
  key_links:
    - from: "frontend/src/components/Terminal/TerminalPane.tsx"
      to: "frontend/src/stores/themeStore.ts"
      via: "useThemeStore hook 订阅 theme 变化"
      pattern: "useThemeStore"
    - from: "frontend/src/components/Terminal/TerminalTabBar.tsx"
      to: "frontend/src/stores/themeStore.ts"
      via: "useThemeStore hook 决定激活 tab 背景色"
      pattern: "useThemeStore"
---

<objective>
实现终端主题与系统亮色/暗色主题联动，确保终端配色、tab 激活背景色、QuickConnectPane 背景色在主题切换时保持视觉一致。

Purpose: 解决亮色主题下终端仍显示深色背景的视觉割裂问题，提供 Solarized Light 护眼配色。
Output: 四个文件修改，终端区域全面响应 themeStore 主题切换。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-终端体验优化/04-RESEARCH.md

@frontend/src/stores/themeStore.ts
@frontend/src/components/Terminal/TerminalPane.tsx
@frontend/src/components/Terminal/TerminalPane.module.css
@frontend/src/components/Terminal/TerminalTabBar.tsx
@frontend/src/components/Terminal/QuickConnectPane.tsx

<interfaces>
<!-- themeStore — 终端主题联动的数据源 -->
From frontend/src/stores/themeStore.ts:
```typescript
type Theme = 'light' | 'dark'
interface ThemeStore {
  theme: Theme
  setTheme: (theme: Theme) => void
}
export const useThemeStore: UseBoundStore<StoreApi<ThemeStore>>
```

From frontend/src/components/Terminal/TerminalTabBar.tsx:
```typescript
export interface TermTab {
  termID: string
  sessionID: string
  hostID: string
  hostName: string
  isHome?: boolean
  status?: 'connected' | 'disconnected'
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: TerminalPane 动态主题 + CSS module 修复</name>
  <files>frontend/src/components/Terminal/TerminalPane.tsx, frontend/src/components/Terminal/TerminalPane.module.css</files>
  <read_first>
    - frontend/src/components/Terminal/TerminalPane.tsx（当前硬编码 theme 在 line 34-37）
    - frontend/src/components/Terminal/TerminalPane.module.css（当前硬编码 background-color: #1e1e2e 在 line 4）
    - frontend/src/stores/themeStore.ts（useThemeStore hook 接口）
  </read_first>
  <action>
1. 在 TerminalPane.tsx 顶部添加 import：
   `import { useThemeStore } from '../../stores/themeStore'`

2. 在文件顶部（组件外）定义两个主题常量对象：
   ```typescript
   const DARK_TERMINAL_THEME = {
     background: '#1e1e2e',
     foreground: '#cdd6f4',
     cursor: '#f5e0dc',
     selectionBackground: '#585b70',
   }

   const LIGHT_TERMINAL_THEME = {
     background: '#fdf6e3',
     foreground: '#657b83',
     cursor: '#586e75',
     selectionBackground: '#eee8d5',
   }
   ```

3. 在组件函数体内（`const [term, setTerm]` 之前）添加：
   `const { theme } = useThemeStore()`

4. 修改 Terminal 初始化（line 31-39），将硬编码 theme 改为动态：
   ```typescript
   const terminal = new Terminal({
     fontSize: 13,
     fontFamily: 'Menlo, Monaco, "Courier New", monospace',
     theme: theme === 'dark' ? { ...DARK_TERMINAL_THEME } : { ...LIGHT_TERMINAL_THEME },
     cursorBlink: true,
   })
   ```
   注意：初始化时需要用展开运算符创建新对象。

5. 在 `// Fit and focus when becoming visible` 注释之前，添加新的 useEffect 响应主题变化：
   ```typescript
   // Update xterm theme when system theme changes
   useEffect(() => {
     if (!termRef.current) return
     const newTheme = theme === 'dark' ? DARK_TERMINAL_THEME : LIGHT_TERMINAL_THEME
     termRef.current.options.theme = { ...newTheme }
   }, [theme])
   ```

6. 在容器 div（line 88-90）上添加动态 style 覆盖 CSS module 的硬编码背景色：
   将 `style={{ display: visible ? 'flex' : 'none', position: 'relative' }}`
   改为 `style={{ display: visible ? 'flex' : 'none', position: 'relative', backgroundColor: theme === 'dark' ? '#1e1e2e' : '#fdf6e3' }}`

7. 修改 TerminalPane.module.css，移除硬编码背景色：
   将 `.container` 中的 `background-color: #1e1e2e;` 删除（由 inline style 动态控制）。
  </action>
  <verify>
    <automated>cd D:/code/github/get-shit-ssh/frontend && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - TerminalPane.tsx 包含 `import { useThemeStore } from '../../stores/themeStore'`
    - TerminalPane.tsx 包含 `DARK_TERMINAL_THEME` 常量，其 background 值为 `'#1e1e2e'`
    - TerminalPane.tsx 包含 `LIGHT_TERMINAL_THEME` 常量，其 background 值为 `'#fdf6e3'`
    - TerminalPane.tsx 包含 `const { theme } = useThemeStore()`
    - TerminalPane.tsx 包含 `termRef.current.options.theme = { ...newTheme }` 在 useEffect 中
    - TerminalPane.tsx 容器 div 的 style 包含 `backgroundColor: theme === 'dark' ? '#1e1e2e' : '#fdf6e3'`
    - TerminalPane.module.css 的 .container 中不再包含 `background-color: #1e1e2e`
    - TypeScript 编译无错误
  </acceptance_criteria>
  <done>终端在亮色主题下显示 Solarized Light 配色（bg #fdf6e3, fg #657b83），暗色主题保持 Catppuccin Mocha（bg #1e1e2e, fg #cdd6f4），切换主题实时更新</done>
</task>

<task type="auto">
  <name>Task 2: Tab 激活背景色联动 + QuickConnectPane 同步</name>
  <files>frontend/src/components/Terminal/TerminalTabBar.tsx, frontend/src/components/Terminal/QuickConnectPane.tsx</files>
  <read_first>
    - frontend/src/components/Terminal/TerminalTabBar.tsx（line 31-33 硬编码 bg-[#1e1e2e]）
    - frontend/src/components/Terminal/QuickConnectPane.tsx（line 12 硬编码 dark:bg-[#1e1e2e]）
    - frontend/src/stores/themeStore.ts（useThemeStore hook 接口）
  </read_first>
  <action>
1. 修改 TerminalTabBar.tsx：

   a. 添加 import：
      `import { useThemeStore } from '../../stores/themeStore'`

   b. 在 `TerminalTabBar` 函数体内（`if (tabs.length === 0)` 之前）添加：
      ```typescript
      const { theme } = useThemeStore()
      const activeBg = theme === 'dark' ? 'bg-[#1e1e2e]' : 'bg-[#fdf6e3]'
      const activeText = theme === 'dark' ? 'text-green-400' : 'text-[#657b83]'
      ```

   c. 修改 line 31-33 的 className，将硬编码的 `'bg-[#1e1e2e] text-green-400'` 替换为动态值：
      ```typescript
      isActive
        ? `${activeBg} ${activeText}`
        : 'bg-gray-50 dark:bg-gray-800 text-gray-500 dark:text-gray-400 hover:bg-gray-200 dark:hover:bg-gray-700 hover:text-gray-700 dark:hover:text-gray-200'
      ```

2. 修改 QuickConnectPane.tsx：

   a. 添加 import：
      `import { useThemeStore } from '../../stores/themeStore'`

   b. 在函数体内（return 之前）添加：
      `const { theme } = useThemeStore()`

   c. 修改 line 12 的容器 div className，将 `bg-gray-50 dark:bg-[#1e1e2e]` 替换为动态背景：
      将整个 className 改为：
      ```
      className={`flex flex-col items-center justify-center h-full text-gray-700 dark:text-gray-300 px-8 gap-6 ${theme === 'dark' ? 'bg-[#1e1e2e]' : 'bg-[#fdf6e3]'}`}
      ```
  </action>
  <verify>
    <automated>cd D:/code/github/get-shit-ssh/frontend && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - TerminalTabBar.tsx 包含 `import { useThemeStore } from '../../stores/themeStore'`
    - TerminalTabBar.tsx 包含 `const { theme } = useThemeStore()`
    - TerminalTabBar.tsx 包含 `const activeBg = theme === 'dark' ? 'bg-[#1e1e2e]' : 'bg-[#fdf6e3]'`
    - TerminalTabBar.tsx 不再包含硬编码的 `'bg-[#1e1e2e] text-green-400'` 字符串
    - TerminalTabBar.tsx 激活 tab 使用 `${activeBg} ${activeText}` 模板字符串
    - QuickConnectPane.tsx 包含 `import { useThemeStore } from '../../stores/themeStore'`
    - QuickConnectPane.tsx 包含 `const { theme } = useThemeStore()`
    - QuickConnectPane.tsx 容器 div 不再包含 `bg-gray-50 dark:bg-[#1e1e2e]`
    - QuickConnectPane.tsx 容器 div 包含 `theme === 'dark' ? 'bg-[#1e1e2e]' : 'bg-[#fdf6e3]'`
    - TypeScript 编译无错误
  </acceptance_criteria>
  <done>激活 tab 背景色在亮色主题下为 #fdf6e3、暗色下为 #1e1e2e，与终端背景完全一致；QuickConnectPane 背景色同步联动</done>
</task>

</tasks>

<verification>
1. `cd frontend && npx tsc --noEmit` — TypeScript 编译无错误
2. 手动验证：启动应用，切换到亮色主题，终端背景变为米白色 #fdf6e3，文字为 #657b83
3. 手动验证：激活 tab 背景色与终端背景色一致
4. 手动验证：切换回暗色主题，终端恢复 #1e1e2e 深色背景
5. 手动验证：QuickConnectPane（新连接 tab）背景色跟随主题
</verification>

<success_criteria>
- 亮色主题：终端 bg=#fdf6e3, fg=#657b83, cursor=#586e75, selection=#eee8d5
- 暗色主题：终端 bg=#1e1e2e, fg=#cdd6f4, cursor=#f5e0dc, selection=#585b70
- 激活 tab 背景色与终端背景色完全一致
- QuickConnectPane 背景色与终端主题同步
- 主题切换实时生效，无需重连
- TypeScript 编译通过
</success_criteria>

<output>
After completion, create `.planning/phases/04-终端体验优化/04-01-SUMMARY.md`
</output>
